import { CircularProgress } from "@mui/material";

const CustomCircularProgressBar = () => {
	return <CircularProgress size="1.5rem" sx={ {
		color: "whitesmoke",
	} }/>;
};

export default CustomCircularProgressBar;