import { createTheme } from '@mui/material/styles';

// This is an attempt, probably will not use it like this
const theme = createTheme({
	palette: {
		primary: {
			main: '#0A1717',
			contrastText: '#D9D9D9',
		},
		secondary: {
			main: '#a8a8a8',
		},
	},
});

export default theme;
